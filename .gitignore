# Claude MCP config (contains secrets)
.claude/mcp.json

# API keys config (contains secrets — generated at build time)
app/js/config.js

# Local environment variables (NEVER commit this)
.env.local
.env

# Node / Capacitor build artifacts
node_modules/
android/.gradle/
android/build/
android/app/build/
.capacitor/
