# Claude MCP config (contains secrets)
.claude/mcp.json

# API keys config — all values are public/publishable (safe to commit)
# app/js/config.js

# Local environment variables (NEVER commit this)
.env.local
.env

# Supabase CLI cache
supabase/.temp/
