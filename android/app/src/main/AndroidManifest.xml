<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Camera & Media -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- The microphone, for dictating the vitals and for voice notes in
         Messages. getUserMedia inside the WebView needs this declared, so
         without it recording works in a browser and fails in the APK — which
         is what the voice-note button has been doing. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Capacitor's WebView asks for RECORD_AUDIO *and* MODIFY_AUDIO_SETTINGS
         together when a page calls getUserMedia, and only lets the recording
         start when both come back granted. A permission that is not declared
         here can never be granted, so leaving this out made getUserMedia fail
         in the APK while working perfectly in a browser — which is exactly
         what the voice-note button has been doing. It is a normal permission:
         Android grants it at install time and never asks the user. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- Notifications & Reminders -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Optional hardware features -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">

            <!-- App launcher intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep linking: homatt://app/... -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="homatt" android:host="app" />
            </intent-filter>

            <!-- The long-press menu on the app icon: the same three targets
                 as the home-screen widget. It must sit on the activity that
                 carries the LAUNCHER intent-filter or Android ignores it. -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />

        </activity>

        <!-- ── The home-screen widget ──────────────────────────────────────
             Three things a clinic does all day, one tap from the home screen.
             `exported="false"` is correct and is what the platform expects
             from API 31: the AppWidget host is privileged and still reaches
             it, while nothing else can make it fire. -->
        <receiver
            android:name=".HomattWidgetProvider"
            android:label="@string/widget_title"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_homatt_info" />
        </receiver>

        <!-- Android's answer to "did they actually add the widget?".
             requestPinAppWidget returns when the DIALOG opens, not when the
             person agrees, so without this the app cannot tell adding from
             declining. exported="false": only the system's own PendingIntent
             fires it. -->
        <receiver
            android:name=".WidgetPinnedReceiver"
            android:exported="false" />

        <!-- Local Notifications receivers -->
        <receiver
            android:name="com.capacitorjs.plugins.localnotifications.TimedNotificationPublisher"
            android:exported="false" />
        <receiver
            android:name="com.capacitorjs.plugins.localnotifications.NotificationRestoreReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- File provider for camera/gallery uploads -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
